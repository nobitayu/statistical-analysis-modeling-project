# 📋 代码使用顺序与输入输出说明

## 🔄 完整工作流程

```
原始数据 → [Step 1] → [Step 2] → [Step 3/3.5] → [Step 4] → [Step 5]
          预处理     VIF检查    模型训练        可视化     评估报告
```

---

## 📝 详细步骤说明

### **Step 1: 数据预处理** 
**文件**: `1_preprocess.py`

#### 输入：
- **必需文件**: `data/raw_data.csv` (原始数据)
- **配置文件**: `config.py` (特征配置)

#### 处理内容：
1. 读取原始数据
2. 目标变量对数化：`log_views = log(views + 1)`
3. 提取特征列（数值型、二值型、分类变量、时段变量）
4. 哑变量处理（剔除基准类，避免完全共线性）
5. 缺失值填充（用0填充）
6. 数据清洗

#### 输出：
- **文件**: `output/processed_data.csv`
  - 包含所有特征列 + `log_views` 目标变量
  - 已处理缺失值和异常值
  - 已剔除基准分类变量

#### 控制台输出：
```
>>> [Step 1] 开始数据预处理...
原始数据形状: (样本数, 列数)
已生成目标变量: log_views
检测到分类变量 (Category): X 个
检测到时段变量 (Period): X 个
处理完成！最终特征数量: X
清洗后的数据已保存至: output/processed_data.csv
```

---

### **Step 2: VIF共线性检查**
**文件**: `2_check_vif.py`

#### 输入：
- **必需文件**: `output/processed_data.csv` (来自Step 1)

#### 处理内容：
1. 读取清洗后的数据
2. 强制类型转换（确保全是数值）
3. 剔除方差为0的常量列
4. 计算每个特征的VIF值
5. 识别高共线性特征（VIF > 10）

#### 输出：
- **无文件输出**（仅控制台输出）

#### 控制台输出：
```
>>> [Step 2] 开始共线性(VIF)检查...
原始特征数量: X
正在计算 VIF (特征数: X)...
========================================
VIF 结果 (VIF > 10 表示严重共线性)
========================================
Feature          VIF
feature_1        15.23
feature_2        8.45
...
========================================
⚠️ 警告: 以下变量存在严重共线性，建议在 config.py 中移除其中一个:
['feature_1', 'feature_3']
```

#### ⚠️ 重要提示：
- **手动操作**：查看VIF结果后，如果发现高共线性特征（VIF > 10），需要**手动**在 `config.py` 中从 `NUMERIC_COLS` 或 `BINARY_COLS` 中删除相关特征
- 删除特征后，**需要重新运行 Step 1** 来更新 `processed_data.csv`

---

### **Step 3: 训练OLS模型** (基础版)
**文件**: `3_train_model.py`

#### 输入：
- **必需文件**: `output/processed_data.csv` (来自Step 1)
- **配置文件**: `config.py` (模型配置)

#### 处理内容：
1. 读取清洗后的数据
2. 数据清洗（类型转换、填充NaN、处理Inf）
3. 特征标准化（可选，由 `config.USE_FEATURE_SCALING` 控制）
4. 训练/测试集划分（80/20）
5. 训练OLS回归模型
6. 交叉验证（可选，由 `config.USE_CROSS_VALIDATION` 控制）
7. 模型评估（R²、RMSE、MAE、MAPE）

#### 输出：
- **模型文件**: `output/ols_model.pkl`
- **标准化器**（如果使用标准化）: `output/ols_model_scaler.pkl`

#### 控制台输出：
```
>>> [Step 3] 开始训练 OLS 回归模型...
正在清洗数据以确保全是数字...
正在标准化特征...（如果开启）
训练集形状: (样本数, 特征数+1)
测试集形状: (样本数, 特征数+1)

>>> 进行5折交叉验证...（如果开启）
交叉验证 R² 得分: 0.xxxx (±0.xxxx)
交叉验证 RMSE:    0.xxxx (±0.xxxx)

##################################################
OLS Regression Results
##################################################
[详细的统计模型报告]
==================================================
[测试集评估结果]
==================================================
R² (决定系数):           0.xxxx
RMSE (均方根误差):        0.xxxx
MAE (平均绝对误差):       0.xxxx
MAPE (平均绝对百分比误差): x.xx%
==================================================

✅ 模型已保存至: output/ols_model.pkl
✅ 标准化器已保存至: output/ols_model_scaler.pkl（如果使用）
```

---

### **Step 3.5: 多模型对比** (推荐！)
**文件**: `3.5_model_comparison.py`

#### 输入：
- **必需文件**: `output/processed_data.csv` (来自Step 1)
- **配置文件**: `config.py` (模型配置)

#### 处理内容：
1. 读取清洗后的数据
2. 数据清洗
3. 特征标准化（必需，正则化模型需要）
4. 训练/测试集划分（80/20）
5. 训练4种模型：
   - OLS（普通最小二乘）
   - Ridge（L2正则化）
   - Lasso（L1正则化，自动特征选择）
   - ElasticNet（L1+L2正则化）
6. 网格搜索自动调优超参数
7. 对比所有模型性能
8. 自动选择并保存最佳模型

#### 输出：
- **最佳模型文件**: 
  - `output/ols_model.pkl` (如果是OLS最佳)
  - `output/ols_model_ridge.pkl` (如果是Ridge最佳)
  - `output/ols_model_lasso.pkl` (如果是Lasso最佳)
  - `output/ols_model_elasticnet.pkl` (如果是ElasticNet最佳)
- **标准化器**: `output/ols_model_*_scaler.pkl`
- **对比结果**: `output/ols_model_comparison.csv`

#### 控制台输出：
```
>>> [Step 3.5] 多模型对比分析...
正在清洗数据...
正在标准化特征...

[1/4] 训练 OLS 模型...
✅ OLS 模型训练完成

[2/4] 训练 Ridge 模型（L2正则化）...
✅ Ridge 模型训练完成 (最优α=10)

[3/4] 训练 Lasso 模型（L1正则化）...
✅ Lasso 模型训练完成 (最优α=0.1, 保留45特征)

[4/4] 训练 ElasticNet 模型（L1+L2正则化）...
✅ ElasticNet 模型训练完成 (最优α=0.1, l1_ratio=0.5)

======================================================================
模型对比结果（按R²排序）
======================================================================
Model                                    R²      RMSE     MAE
Ridge (α=10)                            0.8234   0.4567   0.3456
ElasticNet (α=0.1, l1_ratio=0.5)        0.8201   0.4612   0.3501
OLS                                     0.8198   0.4623   0.3512
Lasso (α=0.1, 保留45/50特征)           0.8156   0.4689   0.3567
======================================================================

🏆 最佳模型: Ridge
✅ Ridge模型已保存至: output/ols_model_ridge.pkl
✅ 标准化器已保存至: output/ols_model_ridge_scaler.pkl
✅ 对比结果已保存至: output/ols_model_comparison.csv
```

---

### **Step 4: 可视化分析**
**文件**: `4_visualization.py`

#### 输入：
- **必需文件**: 
  - `output/processed_data.csv` (来自Step 1)
  - `output/ols_model.pkl` 或 `output/ols_model_*.pkl` (来自Step 3或3.5)
  - `output/ols_model_*_scaler.pkl` (如果模型使用了标准化)

#### 处理内容：
1. 读取数据和模型
2. 自动识别模型类型（statsmodels或sklearn）
3. 自动加载标准化器（如果存在）
4. 生成预测值和残差
5. 生成4种可视化图表

#### 输出：
- **图表文件**（保存在 `output/plots/` 目录）:
  - `predicted_vs_actual.png` - 预测值 vs 实际值散点图
  - `residual_analysis.png` - 残差分析图（残差图 + Q-Q图）
  - `feature_importance.png` - 特征重要性图（Top 20）
  - `residual_distribution.png` - 残差分布直方图

#### 控制台输出：
```
>>> [Step 4] 生成模型可视化分析...
✅ 已加载模型: output/ols_model_ridge.pkl
✅ 已加载标准化器: output/ols_model_ridge_scaler.pkl
正在生成: 预测值 vs 实际值散点图...
✅ 已保存: predicted_vs_actual.png
正在生成: 残差分析图...
✅ 已保存: residual_analysis.png
正在生成: 特征重要性图...
✅ 已保存: feature_importance.png
正在生成: 残差分布直方图...
✅ 已保存: residual_distribution.png

残差统计信息:
  均值: 0.000000
  标准差: 0.xxxxxx
  最小值: -x.xxxx
  最大值: x.xxxx

✅ 所有图表已保存至: output/plots
```

---

### **Step 5: 详细模型评估**
**文件**: `5_model_evaluation.py`

#### 输入：
- **必需文件**: 
  - `output/processed_data.csv` (来自Step 1)
  - `output/ols_model.pkl` 或 `output/ols_model_*.pkl` (来自Step 3或3.5)
  - `output/ols_model_*_scaler.pkl` (如果模型使用了标准化)

#### 处理内容：
1. 读取数据和模型
2. 自动识别模型类型
3. 自动加载标准化器（如果存在）
4. 残差正态性检验（Shapiro-Wilk或Kolmogorov-Smirnov）
5. 异方差检验（Breusch-Pagan，仅statsmodels模型）
6. 特征重要性分析
7. 生成JSON格式评估报告

#### 输出：
- **评估报告**: `output/evaluation_report.json`

#### 控制台输出：
```
>>> [Step 5] 详细模型评估...
✅ 已加载模型: output/ols_model_ridge.pkl
✅ 已加载标准化器: output/ols_model_ridge_scaler.pkl

============================================================
[1] 残差正态性检验
============================================================
检验方法: Shapiro-Wilk
统计量: 0.xxxx
p值: 0.xxxx
✅ 残差符合正态分布 (p > 0.05)

============================================================
[2] 异方差检验 (Breusch-Pagan Test)
============================================================
统计量: x.xxxx
p值: 0.xxxx
✅ 无异方差问题 (p > 0.05)

============================================================
[3] 特征重要性分析 (Top 10)
============================================================
特征              系数      p值
subscriber        0.1234    0.0000
channel_avg_views  0.0987    0.0001
...

显著特征数 (p < 0.05): 45 / 50

============================================================
[4] 模型性能总结
============================================================
R² (决定系数):           0.8234
RMSE (均方根误差):       0.4567
MAE (平均绝对误差):      0.3456
MAPE (平均百分比误差):    12.34%
样本数:                 10000
特征数:                 50

✅ 评估报告已保存至: output/evaluation_report.json
```

---

## 🎯 两种推荐流程

### **流程A: 基础流程（仅OLS模型）**

```bash
python 1_preprocess.py          # Step 1: 数据预处理
python 2_check_vif.py           # Step 2: VIF检查（手动查看，决定是否剔除特征）
python 3_train_model.py         # Step 3: 训练OLS模型
python 4_visualization.py       # Step 4: 生成可视化
python 5_model_evaluation.py    # Step 5: 详细评估
```

### **流程B: 推荐流程（多模型对比）** ⭐

```bash
python 1_preprocess.py          # Step 1: 数据预处理
python 2_check_vif.py           # Step 2: VIF检查（手动查看，决定是否剔除特征）
python 3.5_model_comparison.py  # Step 3.5: 多模型对比（自动选择最佳模型）
python 4_visualization.py       # Step 4: 生成可视化
python 5_model_evaluation.py    # Step 5: 详细评估
```

---

## 📊 文件依赖关系图

```
data/raw_data.csv
    ↓
[Step 1] 1_preprocess.py
    ↓
output/processed_data.csv
    ↓
[Step 2] 2_check_vif.py (仅查看，无文件输出)
    ↓
[手动操作] 在config.py中剔除高VIF特征（可选）
    ↓
[重新运行 Step 1] (如果修改了config.py)
    ↓
output/processed_data.csv (更新后)
    ↓
[Step 3] 3_train_model.py 或 [Step 3.5] 3.5_model_comparison.py
    ↓
output/ols_model*.pkl + output/ols_model*_scaler.pkl (如果使用标准化)
    ↓
[Step 4] 4_visualization.py
    ↓
output/plots/*.png (4个图表文件)
    ↓
[Step 5] 5_model_evaluation.py
    ↓
output/evaluation_report.json
```

---

## ⚠️ 重要注意事项

1. **必须按顺序运行**：每个步骤的输出是下一步的输入
2. **Step 2后可能需要重新运行Step 1**：如果手动在config.py中删除了特征
3. **Step 3和Step 3.5二选一**：
   - Step 3：仅训练OLS模型
   - Step 3.5：对比4种模型，自动选择最佳（推荐）
4. **Step 4和Step 5需要模型文件**：必须先运行Step 3或Step 3.5
5. **标准化器文件**：如果模型使用了标准化，Step 4和Step 5会自动查找并加载scaler文件

---

## 🔧 配置文件说明

所有路径和配置都在 `config.py` 中：

- **数据路径**: `DATA_PATH`, `PROCESSED_DATA_PATH`
- **模型路径**: `MODEL_PATH`
- **特征配置**: `NUMERIC_COLS`, `BINARY_COLS`
- **模型配置**: `USE_FEATURE_SCALING`, `USE_CROSS_VALIDATION`, `CV_FOLDS`, `RANDOM_STATE`
- **输出路径**: `PLOTS_DIR`, `EVALUATION_REPORT_PATH`

